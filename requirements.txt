apify>=1.7.0,<3.0.0
httpx>=0.27.0,<1.0.0
beautifulsoup4>=4.12.0,<5.0.0
pytest>=8.0.0,<9.0.0
# Crawlee (via apify): Pydantic 2.12+ rejects HttpHeaders using both default and default_factory.
pydantic>=2.7.0,<2.12.0
# Crawlee 0.6.x workaround expects browserforge.download.DATA_FILES (removed in 1.2.4).
browserforge==1.2.3
